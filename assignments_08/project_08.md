The database was created successfully.

Link: https://screenrec.com/share/qvcnUT0wrb

- Lightweight compute cost: $21.72
- Heavy analytics workload cost: $32,620.20

I was surprised by how dramatically the estimated cost increased from the lightweight scenario to the heavy analytics workload. The lightweight workload costs only $21.72 because it requires relatively little computing power, storage, and network usage. In comparison, the heavy workload costs $32,620.20 because it uses much more powerful infrastructure, including expensive GPU computing. This comparison demonstrates how cloud services can be customized based on the workload, but it also shows why resources must be selected carefully to control costs. A GPU instance is worth paying for when a workload involves intensive machine learning training or other highly parallel calculations, for smaller pipelines and ordinary data processing, a lower-cost CPU instance is usually fine.